services:
  web:
    image: yiisoftware/yii2-php:8.2-apache
    ports:
      - "6000:80"
    volumes:
      - ./yii2:/app
    env_file:
      - ./yii2/.env
    environment:
      YII_ENV: ${YII_ENV:-dev}
      YII_DEBUG: ${YII_DEBUG:-1}
